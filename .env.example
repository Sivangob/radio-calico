# Radio Calico Environment Configuration

# Node environment (development/production)
NODE_ENV=development

# HTTP server port
PORT=3000

# SQLite database path (development only)
DB_PATH=./database.db

# PostgreSQL configuration (production only)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=radiocalico
POSTGRES_USER=radiocalico
POSTGRES_PASSWORD=your_secure_password_here
