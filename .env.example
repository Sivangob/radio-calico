# Radio Calico Environment Configuration

# Node environment (development/production)
NODE_ENV=development

# HTTP server port
PORT=3000

# Database type override (optional: 'sqlite' or 'postgres')
# If not set, defaults to sqlite in development and postgres in production
# DB_TYPE=sqlite

# SQLite database path (when using SQLite)
DB_PATH=./database.db

# PostgreSQL configuration (when using PostgreSQL)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=radiocalico
POSTGRES_USER=radiocalico
POSTGRES_PASSWORD=your_secure_password_here
